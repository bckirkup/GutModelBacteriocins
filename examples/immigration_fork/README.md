# Immigration fork

This overlay restores `burn_in.h5`, declares the immigrant strain with
`count: 0` (checkpoint restoration supplies the residents), and injects one
fully wired `ColE1` propagule near the established biomass at the fork's
relative pulse step.
