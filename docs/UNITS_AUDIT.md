# Concentration-Unit Audit

**Scope:** audit only. This branch changes no implementation, default, Kd, Km,
or simulation behavior. Values and classifications below describe the current
`main` behavior after the merged physics work.

## Executive summary

The receptor/ligand parameterization has two conventions:

1. The B12/siderophore ligand fields and their receptor Kd values are mostly
   numerical molar values (`M`) carrying a `mol/m^3` label. Within each
   receptor competition ratio, that convention is self-consistent.
2. QSSA bacteriocin fields are genuine `mol/m^3`: source rates are in `mol/s`
   and the Green's function divides by diffusivity and distance. The receptor
   code compares those fields directly with the numerical-molar toxin Kd
   values. This makes toxin occupancy approximately 1000 times too high in
   the intended physical interpretation.

The clearest documented field error is B12: the documented target is 1 uM,
but `1e-6` is 1 uM expressed as mol/L, not as mol/m^3. The true mol/m^3 value
is `1e-3`. The B12 field and BtuB ligand Kd currently preserve their intended
ratio (`1e-6 / 1e-9 = 1000`), so changing only either one would change
competition.

## Unit reference

| Quantity | Equivalent value |
|---|---:|
| 1 M | 1000 mol/m^3 |
| 1 mM | 1 mol/m^3 |
| 1 uM | 1e-3 mol/m^3 |
| 1 nM | 1e-6 mol/m^3 |

## 1. Chemical-species fields

`ChemicalSpec` explicitly declares `initial_conc` and `boundary_conc` as
mol/m^3 (`src/fields/chemical_field.h:18-24`). The default table is in
`src/io/input_parser.cpp:113-123`.

| Field/species | Current initial/boundary | Stated biological target | True mol/m^3 target | Classification and consumers |
|---|---:|---|---:|---|
| Carbon | `5e-3` | The VBF documentation describes an approximately 1 mM bulk equilibrium (`docs/PARAMETERS.md:166`, `204-205`), while the table value is a 5 mM boundary/background value | 1 mM = `1`; 5 mM = `5` | **Self-consistent units; target/config relationship differs.** Carbon is compared with `agent.km.km_carbon = 5e-3` at `fix_metabolism.cpp:248,251` and with the VBF sink Km `1e-4` in `src/fields/vbf.cpp`/`docs/PARAMETERS.md:204-205`. |
| Iron | `1e-4` | No unambiguous ambient free-iron target is stated. The value is numerically 100 uM if it is mol/m^3. | 100 uM = `1e-4` | **Unclear biological target, not a demonstrated unit slip.** It is consumed by Fur and iron Monod terms (`fix_metabolism.cpp:197-203,241-245`), and is also used as the ligand for FepA and FhuA toxin competition (`fix_receptor.cpp:118-124,162-168`). |
| B12/corrinoid | `1e-6` | Approximately 1 uM total bioavailable corrinoid (`docs/RECEPTOR_LIGAND_PARAMETERIZATION.md:28,38`; `docs/PARAMETERS.md:169-185`) | 1 uM = `1e-3` | **Wrong label/value convention.** The field is constant and not depleted (`fix_metabolism.cpp:371-375`), but it enters BtuB competition (`fix_receptor.cpp:95-103`) and B12 Monod growth (`fix_metabolism.cpp:197-199,247,252`). |
| Bacteriocin BtuB/FepA/CirA/FhuA | `0` initial/boundary | No environmental background; supplied by QSSA | `0` background | **Self-consistent field initialization.** QSSA deposits positive toxin concentrations into these fields; see §5. |
| Acetate | `80` | 60–100 mM physiology (`docs/PARAMETERS.md:313`) | 80 mM = `80` | **Self-consistent.** Compared with `metE_acetate_km = 40` at `fix_metabolism.cpp:274-279`. |
| Ethanolamine | `0.5e-3` | No conflicting target in the source documentation; value is 0.5 mM | 0.5 mM = `0.5` | **Self-consistent units; target provenance unclear.** Compared with `eut_km = 0.1e-3` at `fix_metabolism.cpp:281-284`. |
| Siderophore/enterobactin | Not in the static table; dynamically added as `0` when enabled (`src/io/input_parser.cpp:220-224`) | No ambient target; secretion is `mol/s/cell` | `0` background; dynamic field is generated in mol/m^3 | **Field units self-consistent, but its receptor Kd and reimport Km use the suspect numerical-nanomolar convention.** Used at `fix_receptor.cpp:141-149` and `fix_metabolism.cpp:322-349`. |

The static-table diffusivities and rates do not imply concentration units. In
particular, the bacteriocin entries have zero background and
`diffusion_enabled=false`; their concentrations are generated by QSSA, not by
the table's initial values.

## 2. Receptor Kd consumers

The eight Kd defaults are declared in `src/fixes/fix_receptor.h:27-40`.
The CPU implementation constructs

```text
apparent_Kd = (kd_tox / toxin_affinity)
              * (1 + ligand_conc / (kd_ligand / ligand_affinity))
```

at `src/fixes/fix_receptor.cpp:44-65`.

| Kd | Compared toxin field | Compared ligand field | CPU use | Classification |
|---|---|---|---|---|
| `kd_colicinE_btuB = 5e-10` | `BACTERIOCIN_BTUB` | B12 (`chem.conc`) | `fix_receptor.cpp:91-111` | **Wrong across the toxin/field boundary; self-consistent for ligand competition.** Toxin is genuine mol/m^3; toxin Kd is numerical molar. |
| `kd_b12_btuB = 1e-9` | — | B12 | `fix_receptor.cpp:95-103` | **Self-consistent ratio internally, wrong unit label.** B12/Kd is 1000 in the current convention; both would need the same scale under Option A. |
| `kd_colicinB_fepA = 2e-9` | `BACTERIOCIN_FEPA` | Iron field (used as ferric-enterobactin proxy) | `fix_receptor.cpp:114-134` | **Wrong across toxin/field boundary; ligand identity is also unclear.** The field is iron, while this Kd is documented for ferric enterobactin. |
| `kd_enterobactin = 1e-9` | — | Iron field | `fix_receptor.cpp:118-126` | **Wrong/unclear.** Numerical Kd is described as 1 nM but labeled mol/m^3; the code does not have an enterobactin-Fe field distinct from iron. |
| `kd_colicinIa_cirA = 3e-9` | `BACTERIOCIN_CIRA` | `0.3 * SIDEROPHORE` | `fix_receptor.cpp:137-159` | **Wrong across toxin/field boundary when siderophore is enabled.** |
| `kd_lin_enterobactin = 5e-8` | — | `0.3 * SIDEROPHORE` | `fix_receptor.cpp:141-151` | **Wrong/unclear across field boundary.** It is a numerical 50 nM value but is compared with a genuine mol/m^3 siderophore field. |
| `kd_colicinM_fhuA = 2.5e-9` | `BACTERIOCIN_FHUA` | Iron field (used as ferrichrome proxy) | `fix_receptor.cpp:162-180` | **Wrong across toxin/field boundary; ligand identity is unclear.** |
| `kd_ferrichrome = 1e-8` | — | Iron field | `fix_receptor.cpp:164-172` | **Wrong/unclear.** Ferrichrome is not separately represented; the code uses iron. |

`toxin_occupancy` has no conversion at its entry point: `tox_conc` is used
directly in the denominator at `fix_receptor.cpp:44-65`. Therefore the
approximately 1000-fold toxin potency error is present for every nonzero
toxin/receptor path, on CPU and GPU.

## 3. Monod and Km consumers

The important distinction is that not every small number is suspect. Several
iron and environmental Kms are already numerically expressed in genuine
mol/m^3.

| Constant | Field compared | Consumer | Classification |
|---|---|---|---|
| `Agent::km.km_b12 = 1e-9` (`src/core/agent.cpp:51-53`) | B12 field (`1e-6`) | `fix_metabolism.cpp:247,252`; GPU `agent_update_kernel.cu:64,67` | **Self-consistent only under the current internal molar convention; unit label/physical interpretation is wrong.** Scale both field and Km together to preserve growth. |
| `Agent::km.km_carbon = 5e-3` | Carbon field | `fix_metabolism.cpp:248,251`; GPU `agent_update_kernel.cu:65-66` | **Self-consistent genuine mol/m^3 value** (5 mM). |
| `Agent::km.km_iron = 1e-6` | None in the growth calculation | MPI transfer `src/core/agent_transfer.cpp` and agent serialization | **Unclear/dead for current metabolism.** The active iron Kms are in `MetabolismConfig`; this per-agent field is transferred but not consumed by the shown Monod path. |
| `km_iron_primary = 10e-6` | Iron field | `fix_metabolism.cpp:238-245`; GPU `agent_update_kernel.cu:57-62` | **Self-consistent genuine mol/m^3.** 10 nM equals `1e-5 mol/m^3`. Do not multiply this by 1000 merely because it is written in scientific notation. |
| `km_iron_iroN = 50e-6` | Iron field | same | **Self-consistent genuine mol/m^3** (50 nM). |
| `km_iron_iutA = 100e-6` | Iron field | same | **Self-consistent genuine mol/m^3** (100 nM). |
| `km_iron_fiu = 200e-6` | Iron field | same | **Self-consistent genuine mol/m^3** (200 nM). |
| `fur.Km = 1e-5` | Iron field | `fix_metabolism.cpp:201-210`; GPU expression update mirrors this through host-side Fur update | **Self-consistent genuine mol/m^3** (10 nM). |
| `siderophore.Km_reimport = 1e-9` (`src/io/chem_environment_config.h:60-66`) | Siderophore field | `fix_metabolism.cpp:340-349`; no separate GPU siderophore reimport path | **Wrong or unclear.** It is labeled mol/m^3 but numerically resembles a 1 nM molar value; genuine 1 nM would be `1e-6 mol/m^3`. `docs/PARAMETERS.md:444` independently lists `1e-6`, so documentation and code disagree. |
| `vbf_carbon_sink_km = 1e-4` | Carbon field | VBF carbon sink | **Self-consistent genuine mol/m^3.** |
| `mucin.Km_degradation = 1e-3` | Mucin field | `src/fields/vbf.cpp:18-22` | **Self-consistent/unclear biological target**, no receptor-unit coupling. |
| `oxygen.Km = 1e-6` | Oxygen field | `fix_metabolism.cpp:256-261`; GPU `agent_update_kernel.cu:71-75` | **Self-consistent as a mol/m^3 field/Km pair; separate subsystem.** |
| `metE_acetate_km = 40` | Acetate field | `fix_metabolism.cpp:274-279`; GPU `agent_update_kernel.cu:80-84` | **Self-consistent genuine mol/m^3** (40 mM). |
| `acetate.scavenge_Km = 5` | Acetate field | `fix_metabolism.cpp:386-389` | **Self-consistent genuine mol/m^3**; this is an acetate scavenging path, not receptor competition. |
| `eut_km = 0.1e-3` | Ethanolamine field | `fix_metabolism.cpp:281-284`; GPU `agent_update_kernel.cu:86-88` | **Self-consistent numerically, but biological annotation is unclear:** `0.1e-3 mol/m^3` is 0.1 uM, not 0.1 mM. |
| `quorum_sensing.lsr_km = 1e-7` | AI-2 field | `src/fixes/fix_quorum_sensing.cpp:30-40` and `tests/test_quorum_sensing.cpp:41,197-221` | **Separate convention/unclear.** AI-2 is produced and stored as a chemical field; the absolute test seed `1e-6` should not be interpreted as a receptor/Kd default without a biological target. |

Thus Option A does **not** mean “multiply every Km by 1000.” The active iron,
carbon, oxygen, acetate, ethanolamine, mucin, and VBF Kms are already in
genuine field units. Only constants proven to share the numerical-molar
convention should be rescaled, and the affected set is not uniform.

## 4. GPU mirrors

| GPU location | Data/path | Audit result |
|---|---|---|
| `src/gpu/receptor_gpu.cpp:101-116` | Uploads all eight receptor Kds and passes chemical toxin, B12, iron, and siderophore arrays | **Exact CPU mirror.** Any receptor conversion must be applied identically. |
| `src/gpu/receptor_kernel.cu:9-18` | Computes the same effective Kds, competition factor, and direct toxin occupancy | **Contains the same toxin/field unit mismatch.** |
| `src/gpu/receptor_kernel.cu:61-103` | Reads toxin and ligand arrays directly; maps BtuB→B12, FepA/FhuA→iron, CirA→siderophore | **Exact CPU field mapping, including the iron/ferrichrome identity ambiguity.** |
| `src/gpu/agent_pool_gpu.cpp:46-92,162-173` | Uploads per-agent `km_b12` and `km_carbon`; passes configured iron Kms | **Mirrors CPU metabolism.** |
| `src/gpu/agent_pool_gpu.h:53-88` | Device buffers for `km_b12` and `km_carbon` | **No hidden conversion.** |
| `src/gpu/agent_update_kernel.cu:39-88` | Applies B12, carbon, iron, oxygen, acetate, and ethanolamine ratios | **Same ratio dependencies as CPU.** |
| `src/gpu/vbf_gpu.cpp:44-58`, `src/gpu/chemistry_kernel.cu:8-48` | Mirrors VBF carbon-sink and mucin-degradation concentration/Km ratios | **Same genuine mol/m^3 environmental convention; no receptor-unit conversion belongs here.** |
| `src/gpu/chemical_field_gpu.*` and QSSA sync at `qssa_solver.cpp:408-412,441-457` | Copies toxin fields between device and host | **No unit conversion.** This is correct for storage, but means the receptor kernel sees the same genuine mol/m^3 QSSA field as CPU. |

## 5. QSSA-to-chemical-field crossing

The crossing is direct:

1. `QSSASolver::solve_bacteriocin_field` gathers burst and continuous sources
   (`src/diffusion/qssa_solver.cpp:336-355`).
2. The Green's function computes concentrations from source rate in mol/s,
   diffusivity in m^2/s, and distance (`qssa_solver.cpp:374-381` and
   `src/diffusion/greens_function.cpp`).
3. `deposit_to_chemical_field` writes the resulting values directly into the
   toxin `ChemicalField` (`qssa_solver.cpp:195-203,374-382`).
4. `FixReceptor::local_toxin_conc` reads the field directly
   (`fix_receptor.cpp:81-84`), and `toxin_occupancy` compares it directly with
   Kd-derived `apparent_kd` (`fix_receptor.cpp:44-65`).
5. GPU QSSA synchronizes the same concentrations to the device
   (`qssa_solver.cpp:408-412,441-457`), and the GPU receptor kernel consumes
   them directly (`receptor_kernel.cu:61-103`).

There is no conversion at this boundary. This is the single cleanly
identified cross-convention defect. Nutrient fields do not cross this QSSA
boundary; they are initialized and evolved as chemical fields.

## 6. Absolute-concentration tests and fixtures

The following tests pin absolute values. Most are deliberately synthetic
stress magnitudes or zero-routing assertions, not biological defaults.

| Test/location | Absolute values | What it encodes |
|---|---|---|
| `tests/test_receptor.cpp:53-56,66,85-90,111-117,137-144` | Toxin `1e-4` or `1e-7`; B12 `0` or `1`; CirA toxin `1e-4`; siderophore `1e-3` | Receptor monotonicity, immunity, ligand competition, and routing. **Ratio-sensitive**, not a biological baseline. |
| `tests/test_per_receptor_toxin.cpp:123-143,164-179` | Toxins `1e-4` and `1e-3`, including explicit zero | Per-receptor routing and kill activation. **Stress values/zero routing**, not a default concentration golden. |
| `tests/test_fur.cpp:48-74,101-116` | Iron `0` versus `1`; toxin `1e-4`; B12 `0` | Fur low/high ordering and susceptibility. **Ratio/order test**, not an ambient iron target. |
| `tests/test_quorum_sensing.cpp:196-221` | AI-2 field `1e-6` | AI-2 import sensitivity. **Absolute test seed**; separate subsystem, no documented biological target in this audit. |
| `tests/test_z_gradient.cpp` | Synthetic carbon profile and configured boundary values | Verifies exponential gradient and boundary behavior. **Transport-unit test**, not a receptor convention. |
| `tests/test_protease_decay.cpp` and `tests/test_greens_function.cpp` | Synthetic source rates and concentrations | Kernel regression and decay-length tests. **Physical dimensional tests**, not Kd baselines. |
| `python/tests/fixtures/eari_vadi_ci_golden.json` | Population-level metrics, no raw concentration | Does not encode a concentration convention directly. |
| `python/tests/fixtures/eari_vadi_ci_fish_golden.json` | Detection/population metrics, no raw concentration | Does not encode a concentration convention directly. |

No test or Python golden found in this inventory asserts the default B12
`1e-6` as a biological concentration. Tests that use toxin magnitudes would
change only if receptor conversion behavior were implemented; this audit
does not change them.

## 7. Accidental cancellation and sweep fragility

At the current defaults, the model is in a sensible graded regime only
because two independent unit errors nearly cancel:

1. The toxin Kd is numerically molar while the QSSA toxin field is genuine
   mol/m^3, making the toxin Kd 1000 times too small in the comparison.
2. The B12 field and BtuB ligand Kd use the same numerical-molar convention,
   making the competition factor 1001 rather than the physically corresponding
   factor for a 1 uM field and 1 nM Kd.

Consequently,

```text
apparent_Kd = 5e-10 * 1001 = 5.005e-7 mol/m^3
```

lands close to the intended apparent Kd by accident. The graded occupancies
at 10/50/100 um are therefore not evidence that the units are correctly
constructed; they are evidence that the two errors happen to compensate at
this one parameter point.

The cancellation is fragile along the exact sweep recommended by
`docs/RECEPTOR_LIGAND_PARAMETERIZATION.md:47-51`. Holding the B12 field at
`1e-6` and sweeping `kd_b12_btuB` changes the competition factor and apparent
Kd as follows:

| `kd_b12_btuB` | Competition factor `1 + [B12]/Kd` | Apparent ColE1 Kd (mol/m^3) | Occupancy at 10 um | Occupancy at 50 um | Occupancy at 100 um |
|---:|---:|---:|---:|---:|---:|
| `1e-9` | `1001` | `5.005e-7` | `0.89831` | `0.42349` | `0.10923` |
| `1e-8` | `101` | `5.05e-8` | `0.98871` | `0.87923` | `0.54861` |
| `1e-7` | `11` | `5.5e-9` | `0.99876` | `0.98526` | `0.91776` |
| `1e-6` | `2` | `1e-9` | `0.99977` | `0.99729` | `0.98397` |

At the proposed `1e-6` endpoint, the 50 um field
(`3.676568e-7 mol/m^3`) is approximately 368 times the apparent Kd, so
occupancy is `0.99729`: the model is effectively re-saturated. A user
following the documented sweep would therefore silently move from the
accidentally graded regime back toward the saturation behavior associated
with the old toxin over-potency, while believing they were varying only
corrinoid affinity. This is why the unit issue is a scientific correctness
problem, not merely a documentation/tidiness problem.

## 8. Option A versus Option B

### Option A: genuine mol/m^3 everywhere

This is the scientifically honest long-term representation, but the blanket
statement “multiply affected fields, Kds, and Kms by 1000 and get only a
1000-fold toxin loosening” is not true without a carefully scoped inventory.

Ratios that *would* be preserved if both operands are converted:

- B12 field / BtuB ligand Kd: `1e-6 / 1e-9 = 1000`;
- B12 field / `Agent::km_b12`: `1e-6 / 1e-9 = 1000`;
- siderophore field / CirA ligand Kd, once a nonzero field exists;
- toxin Kd / ligand Kd within each receptor model, if both are converted.

Ratios that must **not** be blindly scaled:

- iron field versus `km_iron_*`: the Kms are already genuine mol/m^3
  representations of 10/50/100/200 nM;
- iron field versus `fur.Km`: both are already genuine mol/m^3;
- carbon, oxygen, acetate, ethanolamine, mucin, and VBF sink ratios;
- any AI-2 quantity until its biological unit convention is established.

Option A therefore requires a broader, explicit migration of the B12,
receptor-Kd, and suspect siderophore parameters, plus tests for every
metabolic ratio. It would remove the documented unit lie and make future
source/field coupling safer. Its net intended physical effect can be limited
to toxin potency only, but only if the already-correct nutrient Kms are left
unchanged and all affected B12/siderophore ratios are migrated together.

### Option B: convert the toxin field once at receptor entry

This is the minimal behavioral fix: retain the receptor/ligand numerical-molar
convention and explicitly convert each QSSA toxin field by `1e-3` before
comparing it with the Kd values (equivalently, convert the toxin to the
internal numerical convention). It preserves all current ligand and
metabolic ratios and changes only toxin potency. It leaves the misleading
`mol/m^3` labels in place, however, and does not resolve the independent
B12 documentation/default error or the suspect siderophore reimport Km.

### Recommendation

Use **Option A as the end-state**, but implement it as a staged migration
with explicit parameter groups and regression tests. The decisive reason is
that the current post-#208 behavior is defensible only at one accidental
parameter point: the toxin-Kd error and B12-competition error cancel there,
but the documented Kd sweep immediately destroys that cancellation. The first
parameter sweep would therefore silently reproduce toxin saturation while
appearing to explore corrinoid affinity.

First document and test the current conventions; then convert B12 and the
receptor ligand/toxin Kds together, convert `Agent::km_b12` with the B12
field, and resolve the siderophore field/Km and iron-ligand identity
questions. Do not rescale the already-correct iron, carbon, oxygen, acetate,
ethanolamine, mucin, or VBF Kms.

If a narrowly scoped production fix is needed before that migration is
complete, Option B is safer operationally because it has one boundary and
provably preserves all existing ligand/Km ratios. Neither option is
implemented in this audit branch.

## 9. ColE1 BtuB occupancy estimate

This calculation uses the merged finite-burst and screened kernel as a
stationary point-source snapshot:

- burst inventory `M = 1e5 / N_A = 1.660539067e-19 mol`;
- release time `tau = 300 s`;
- initial source rate `Q = M/tau = 5.535130224e-22 mol/s`;
- `D_eff = 8e-13 m^2/s`;
- ColE1 half-life 1800 s, so `k = ln(2)/1800`;
- screening length `ell = sqrt(D_eff/k) = 45.5794 um`;
- toxin Kd `5e-10` in the current numerical-molar convention;
- B12 field `1e-6`, B12 Kd `1e-9`, so the actual competition factor is
  `1 + 1e-6/1e-9 = 1001`;
- apparent toxin Kd after competition:
  `5e-10 * 1001 = 5.005e-7 mol/m^3`;
- receptor expression, toxin affinity, and ligand affinity are all taken as
  one; no immunity factor is part of occupancy itself.

For zero advection, the raw field is

```text
C(r) = Q / (4*pi*D_eff*r) * exp(-r/ell)
```

and occupancy is `C/(apparent_Kd + C)`.

| Distance | Raw toxin field (mol/m^3) | Competition factor | Apparent Kd (mol/m^3) | BtuB occupancy |
|---:|---:|---:|---:|---:|
| 10 um | `4.421248e-6` | `1001` | `5.005e-7` | **0.89831** |
| 50 um | `3.676568e-7` | `1001` | `5.005e-7` | **0.42349** |
| 100 um | `6.137581e-8` | `1001` | `5.005e-7` | **0.10923** |

These values confirm the preliminary `0.90 / 0.42 / 0.11` estimate; including
the actual B12 competition term produces essentially the same values because
the stated `5e-7` effective Kd already approximated the factor of 1000.
The values are not a time-integrated dose and not a full simulation outcome:
they use the initial `Q=M/tau` snapshot, a stationary point source, and zero
advection. The full code additionally depends on source age, source-target
orientation, local advection, boundaries, cutoff/FMM choice, receptor
expression, and immunity.

This supersedes the residual-saturation statement in the description of
physics PR #208, which compared the 50 um field with the bare toxin Kd and
reported approximately `740 * Kd`. That comparison omitted ligand
competition. The corrected statement is that the competition-adjusted
apparent Kd is `5.005e-7 mol/m^3`, placing the 50 um field at occupancy
`0.42349`, not at near-total saturation.

## Conclusion

The receptor/ligand block is internally ratio-consistent but mislabeled; the
QSSA toxin field is genuine mol/m^3 and is the outlier at the receptor
boundary. B12 is the clearest documented 1000-fold field-value error.
Iron, carbon, and most metabolism Kms are not interchangeable examples of
that error: several are already genuine mol/m^3 and must not be blindly
rescaled. No behavior or defaults were changed in this audit.
